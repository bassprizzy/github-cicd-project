name: Secrets Demo

on:
  push:
    branches:
      - main
    paths:
      - 'secrets/**' # Triggers only when files in the secrets directory change

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use secret in deployment
        run: |
          echo "Using secret in deployment......"
          echo "Secret value: ${{ secrets.MY_SECRET }}"

          # Access the secret value using:
          # ${{ secrets.MY_SECRET }}

          # Add deployment command here, e.g.:
          # scp, rsync

